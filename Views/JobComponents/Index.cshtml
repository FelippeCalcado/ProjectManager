@model IEnumerable<ProjectManager.Models.JobComponent>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Job)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Component)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.Job.JobName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Component.JobName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.JobComponentID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.JobComponentID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.JobComponentID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
